import Foundation

/// Window configuration for the main application window.
///
/// `MainWindow` defines the window identifier and display name
/// for the primary Triton application window.
public enum MainWindow {

    public static let name = "Triton"
    public static let id = "main"
}
